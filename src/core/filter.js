define([
	'gitjs/core/eventEmitter',
	'gitjs/util/util'
],function(EventEmitter, u){
	var
		Filter = EventEmitter._$extend({
			
			__init: function(){

				this.__super();

				
			}

			

		});

	return Filter;
})